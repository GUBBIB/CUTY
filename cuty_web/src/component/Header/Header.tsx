import { Link } from "react-router-dom";

const Header = () => {

    return(
        <div id="Header">
            <Link to="/">header test header</Link>
        </div>
    );
}

export default Header;